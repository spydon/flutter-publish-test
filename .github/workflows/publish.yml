name: Publish package
on:
  workflow_dispatch:

jobs:
  publish-package:
    name: Publish package
    runs-on: [ ubuntu-latest ]
    steps:
      - uses: spydon/flutter-publish@main
        with:
          publish: true
